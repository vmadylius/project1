name: CI/CD Pipeline

# Тригери для автоматичного запуску пайплайну
on:
  push:
    branches:
      - main
      - develop
  pull_request:
    branches:
      - main
      - develop

jobs:
  # Job для збірки та тестування
  build-and-test:
    name: Build and Test
    runs-on: ubuntu-latest

    steps:
      # Крок 1: Отримання коду з репозиторію
      - name: Checkout repository
        uses: actions/checkout@v4

      # Крок 2: Налаштування Node.js
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'

      # Крок 3: Чисте встановлення залежностей
      - name: Install dependencies
        run: npm install

      # Крок 4: Статичний аналіз коду лінтером
      - name: Run linter
        run: npm run lint

      # Крок 5: Запуск модульних тестів торгової панелі
      - name: Run unit tests
        run: npm run test:unit

      # Крок 6: Тестова збірка під Production у хмарі
      - name: Build for production
        run: npm run build